const { iteratee } = require("lodash");
const XModem = require("../src/fileTransfer/xmodem");

describe("XModem", () => {
	beforeEach(() => {});

	afterEach(() => {});

	// Add an empty test for now
	it("should always pass", () => {
		expect(true).toBe(true);
	});

});
